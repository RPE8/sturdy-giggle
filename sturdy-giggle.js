class Sturdy {
	constructor({ parent, rowHeight, rowCount, cellRenderer } = {}) {
		this.parent = parent;
		this.rowHeight = rowHeight;
		this.rowCount = rowCount;
		this.cellRenderer = cellRenderer;
	}
}

export default Sturdy;
